apiVersion: k8s.starter.org/v1alpha1
kind: KubernetesCluster
metadata:
  name: testy3
  namespace: default
spec:
  id: testy3
  parameters:
    nodes:
      count: 1
      size: small
    services:
      operators:
        prometheus:
          version: "34.5.1"
  writeConnectionSecretToRef:
    name: testy3-kubeconfig
