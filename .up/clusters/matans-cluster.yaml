apiVersion: k8s.starter.org/v1alpha1
kind: KubernetesCluster
metadata:
  name: matans-cluster
  namespace: default
spec:
  id: matans-cluster
  parameters:
    nodes:
      count: 1
      size: small
    services:
      operators:
        prometheus:
          version: "34.5.1"
  writeConnectionSecretToRef:
    name: matans-cluster-kubeconfig
