apiVersion: k8s.starter.org/v1alpha1
kind: KubernetesCluster
metadata:
  name: morp-test-deny
  namespace: default
spec:
  id: morp-test-deny
  parameters:
    nodes:
      count: 1
      size: small
    services:
      operators:
        prometheus:
          version: "34.5.1"
  writeConnectionSecretToRef:
    name: morp-test-deny-kubeconfig
