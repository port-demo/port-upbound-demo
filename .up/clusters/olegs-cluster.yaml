apiVersion: k8s.starter.org/v1alpha1
kind: KubernetesCluster
metadata:
  name: olegs-cluster
  namespace: default
spec:
  id: olegs-cluster
  parameters:
    nodes:
      count: 1
      size: small
    services:
      operators:
        prometheus:
          version: "34.5.1"
  writeConnectionSecretToRef:
    name: olegs-cluster-kubeconfig
