apiVersion: k8s.starter.org/v1alpha1
kind: KubernetesCluster
metadata:
  name: final-force-request
  namespace: default
spec:
  id: final-force-request
  parameters:
    nodes:
      count: 1
      size: small
    services:
      operators:
        prometheus:
          version: "34.5.1"
  writeConnectionSecretToRef:
    name: final-force-request-kubeconfig
